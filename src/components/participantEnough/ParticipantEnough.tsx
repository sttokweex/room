import './ParticipantEnough.scss';

const ParticipantEnough: React.FC = () => {
  return (
    <div className="participantEnough">
      <div className="mainBlock">Participant&apos;s request...</div>
    </div>
  );
};

export default ParticipantEnough;
